<%- include ('header') -%>
<%- include ('navbar') -%>


    <div class="container">
        <div class="row">

            <div class="col-0 col-sm-0 col-md-3">
            </div>

            <div class="col-sm-12 col-md-6">
                <br />
                <h2>Szerkesztés</h2>
                <form>
                    <div class="form-group">
                        <label for="area1">Bevásárló lista</label>
                        <textarea class="form-control" id="area1" rows="5"><%= post.list %></textarea>
                    </div>

                    <div class="form-group">
                        <label for="area2">Utóirat</label>
                        <textarea class="form-control" id="area2" rows="2"><%= post.ps %></textarea>
                    </div>
                    <br />

                    <a class="btn btn-primary" href="\profile\<%= user._id%>">Közzététel</a>
                    <a class="btn btn-link" href="\profile\<%= user._id%>">Mégse</a>

                </form>
                <br />
            </div>
        </div>
    </div>

<%- include ('footer') -%>